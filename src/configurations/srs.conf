vhost pibbletv {

    srt {
        enabled on;
        listen 10080;  
        latency 200;   
    }

 
    hls {
        enabled on;
        hls_path D:/nginx/html/hls; 
        hls_fragment 4;     
        hls_window 20;      
        hls_cleanup on;     
    }

    # http_hooks {
    #     enabled on;
    #     on_connect http://localhost:8078/validate-stream-key;
    # }
}